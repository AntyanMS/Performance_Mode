# Chuwi / Realtek USB Bluetooth (RTL8852BU и др.)
# enable_autosuspend=0 — не уводить адаптер в USB autosuspend (дополняет TLP USB_EXCLUDE_BTUSB).
# reset=1 — HCI reset при загрузке модуля; иногда улучшает скан после сна.
options btusb enable_autosuspend=0 reset=1
